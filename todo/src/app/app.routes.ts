import { Routes } from '@angular/router';
import { TodoformComponent } from './components/todoform/todoform/todoform.component';

export const routes: Routes = [{ path: '', component: TodoformComponent }];
